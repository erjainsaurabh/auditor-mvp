services:
  flowprobe:
    build: .
    restart: always
    ports:
      - "0.0.0.0:8081:8000"
    env_file:
      - .env
    environment:
      - FLOWPROBE_HEADLESS=true
    volumes:
      - ./flows:/app/flows
      - ./evidence:/app/evidence
