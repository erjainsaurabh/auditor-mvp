# secrets — never in the image
.env

# runtime output — not needed in image
evidence/
report.json

# flows/ is NOT copied into the image at all.
# YAMLs are delivered at runtime via the API.
# Fingerprints and strategy_stats live on the persistent volume.
flows/

# Python artifacts
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# dev / OS noise
.git/
.gitignore
*.md
.DS_Store
